#include <stdio.h>
#include <stdlib.h>
/**
 * main - output all single digit  number of base 10
 *
 * turn: 0 if success
 */
int main(void)
{
	int var;

	for (var = 0; var < 10; var++)
		printf("%d", var);
	printf("\n");

	return (0);
}	
